spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: simple-recruit-api
        ip: 127.0.0.1
      config:
        server-addr: localhost:8848
        namespace: simple-recruit-api
        file-extension: yaml
        group: DEFAULT_GROUP